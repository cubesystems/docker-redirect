server {
    server_name localhost;
    listen 80;

    location /ping {
      return 200 "OK\n";
    }

    location / {
      rewrite (.*) {{ .Env.REDIRECT }}{{ default .Env.REDIRECT_PATH "$1" }} permanent;
    }
}
